# 專案需求：台灣金融政策新聞儀表板

> 本 brief 應與 `dashboard-mockup.html` 一併提供給 Claude Code 參考——
> 該檔案是已經確認定案的視覺設計 mockup（四個畫面：依日期、依議題、
> 趨勢洞察、待審核），前端實作應直接以此檔案的配色、字型、排版、
> 元件樣式為準，不需要重新發想設計。mockup 已包含最新版本的所有
> 功能設計：一篇文章可橫跨多個議題、且每個議題可各自有不同子分類
> （多對多的「議題×子分類」配對，不是單一子分類）；全量人工審核期
> 的待審核卡片支援逐項核准/移除每一組「議題＋子分類」建議、補充
> 議題、整篇文章層級的全部採用/刪除+二次確認；日期與議題畫面皆會
> 並列顯示文章所屬的多個議題標籤；手動新增新聞區塊、分類準確率與
> 爬蟲涵蓋率統計區塊。mockup 中的資料為示意用途，實際資料串接依
> 本文件的資料庫 schema 與 API 設計進行。

## 目標
建立一個每日自動爬取「工商時報」「經濟日報」的儀表板，追蹤金管會「金融卓越發展計畫」
五大主軸相關新聞：亞洲資產管理中心、國際級資本市場、三軌金融、全齡金融、信任金融、
金融大回饋。支援「依日期」「依議題」「趨勢洞察」「待審核」四種檢視模式。

每篇文章採**雙重標籤**分類：先歸入六大議題之一（或多個），再於議題底下歸入三個
子分類之一：**政策與法規／商品與業務／同業動態**。

專案目前處於**全量人工審核期**（過渡期機制，見「審核機制」章節）：所有爬蟲抓到
或手動新增的文章，一律先進入待審核佇列，由使用者人工確認分類後才正式收錄進
`articles` 正式表，供依日期／依議題／趨勢洞察三個檢視使用。規則式分類引擎在此
期間持續運作，但只產生「建議」，不直接寫入正式表——這是為了累積規則準確率數據，
待準確率達標後才逐議題切換為自動分類（不需人工逐篇確認）。

視覺設計已完成定案並實際做過 HTML mockup 確認（見「視覺設計規範」章節），
Claude Code 應直接依照該章節的具體規格實作前端，不需要重新發想設計方向。

## 專案存放與執行方式
- 全部程式碼（爬蟲、分類規則、前端）放在 **GitHub repo**，方便版本控管與逐步調整
- 排程用 **GitHub Actions**（`.github/workflows/daily-crawl.yml`，cron 語法設定每天
  固定時間觸發），由 Actions 執行 `run_daily.py`，執行完把更新後的資料庫/JSON
  commit 回 repo
  - 注意：Claude 本身無法「自己每天醒來執行」，實際觸發排程的是 GitHub Actions
    這類外部機制；Claude Code 的角色是協助寫程式碼、之後遇到爬蟲問題時一起討論
    除錯與調整腳本，不是排程系統本身
- 前端可用 GitHub Pages 直接部署靜態儀表板（讀 repo 裡的 JSON 檔）

## 除錯與迭代機制（重要）
每次排程執行後：
- 產生一份 **執行報告**（`logs/YYYY-MM-DD-report.md`），內容包含：
  - 本次嘗試抓取的關鍵字/來源清單，各自成功/失敗筆數
  - 失敗原因分類（例如：HTTP 錯誤、頁面結構改變導致解析失敗、反爬蟲擋下、
    逾時等），附上失敗的 URL 或關鍵字
  - 新增的「未分類」文章清單（見下方說明）
  - 若有解析邏輯明顯失效（例如抓到的欄位是空的），標記為「需要人工檢視」
- 這份報告是之後回來跟 Claude（不管是 Claude Code 或 Claude.ai）討論調整腳本的
  依據，格式盡量結構化（Markdown 或 JSON 皆可），方便直接貼上來討論
- 期望的討論模式：每次爬蟲遇到問題 → 貼報告內容過來 → 一起列出「問題現象／
  可能原因／改進建議」→ 調整腳本 → 下次驗證

## 資料來源與抓取方式

### 工商時報 (ctee.com.tw)
- 使用內建搜尋頁：`https://www.ctee.com.tw/search/{關鍵字}`
- 每篇文章 HTML head 有結構化的 `meta-keywords`（逗號分隔），可直接當標籤，
  不需要自己做關鍵字抽取
- 文章 URL 格式：`https://www.ctee.com.tw/news/{id}-{catid}`
- **原始文章 URL 必須完整保留並存入資料庫**，前端每則新聞都要能直接點擊連到原文
- 需要禮貌爬取（設定 delay 1-2 秒/請求，加上正常 User-Agent）

### 經濟日報 (udn.com / money.udn.com)
- 財經類文章路徑：`money.udn.com/money/story/{分類碼}/{文章id}`
- 一般新聞路徑：`udn.com/news/story/{分類碼}/{文章id}`
- udn 反爬蟲較嚴，若搜尋頁抓不到，改用 Google site: 搜尋或 RSS 找連結後再單篇 fetch
  （這部分預期會反覆調整，遇到問題就記錄進執行報告一起討論）
- 同樣需要禮貌爬取延遲，原始 URL 必須完整保留

## 追蹤關鍵字（豐富版種子詞庫，可持續擴充）

關鍵字採分層設計：核心詞（一定要抓）+ 擴充詞（提高召回率）。

```python
SEED_TERMS = {
    "核心政策詞": [
        "亞洲資產管理中心", "亞資中心",
        "國際級資本市場",
        "三軌金融",
        "全齡金融",
        "信任金融",
        "金融大回饋",
        "金融卓越發展計畫",
    ],
    "亞資中心延伸詞": [
        "高雄資產管理專區", "高雄亞洲新灣區", "台南專區",
        "OBU", "OSU", "OIU", "OAMU", "境外資產管理平臺",
        "TISA", "個人投資儲蓄投資帳戶", "資產管理規模",
        "亞洲創新籌資平台", "台灣週", "私人銀行", "家族辦公室",
        "五大開放政策",
    ],
    "全齡金融延伸詞": [
        "退休第三支柱", "少子化", "高齡化", "普惠金融",
        "銀髮金融", "長照金融",
    ],
    "信任金融延伸詞": [
        "社會信任", "金融共好", "社會負債", "社會信任資產負債表",
        "永續金融", "公司治理", "ESG金融",
    ],
    "人物與機關": [
        "彭金隆", "童政彰", "金管會", "銀行局",
        "金融發展與亞資中心策進會",
    ],
}
```

## 議題分類規則（純字串比對，不呼叫 LLM，零 API 成本）

比對優先順序：文章的 meta-keywords > 標題 > 前 200 字摘要。
一篇文章可以同時屬於多個議題（multi-tag），不強制單一分類。

```python
TOPICS = {
    "亞洲資產管理中心": [
        "亞洲資產管理中心", "亞資中心", "高雄專區", "高雄資產管理專區",
        "高雄亞洲新灣區", "台南專區", "OBU", "OSU", "OIU", "OAMU",
        "境外資產管理平臺", "TISA", "個人投資儲蓄投資帳戶",
        "資產管理規模", "亞洲創新籌資平台", "台灣週", "私人銀行",
        "家族辦公室", "五大開放政策",
    ],
    "國際級資本市場": [
        "國際級資本市場", "亞洲創新籌資平台", "上市櫃制度", "ETF",
        "跨境合作", "國際板",
    ],
    "三軌金融": ["三軌金融"],
    "全齡金融": [
        "全齡金融", "退休第三支柱", "少子化", "高齡化", "銀髮金融",
        "長照金融", "普惠金融",
    ],
    "信任金融": [
        "信任金融", "社會信任", "金融共好", "社會負債",
        "社會信任資產負債表", "永續金融", "公司治理", "ESG金融",
    ],
    "金融大回饋": ["金融大回饋"],
}
```

無法匹配任何規則的文章，標記為「未分類」，**先不自動丟給 LLM**——
累積成清單，依照下方「未分類文章處理流程」處理。

## 子分類規則（政策與法規／商品與業務／同業動態）

**資料模型說明**：一篇文章可以橫跨多個議題（例如同時屬於「亞洲資產管理
中心」與「國際級資本市場」），且**每個議題底下的子分類可以各自不同**
（例如同一篇文章在「亞洲資產管理中心」下屬於「政策與法規」，在「國際級
資本市場」下卻屬於「同業動態」）——因為文章在不同議題脈絡下，該議題
關注的面向可能不同。因此分類的最小單位不是「一篇文章」，而是「一篇
文章 × 一個議題」的組合，每個組合各自判定一個子分類。

判斷邏輯：
1. 先用 TOPICS 規則判斷文章屬於哪些議題（可能多個，見「議題分類規則」
   章節）
2. **對文章命中的每一個議題分別跑一次 SUBCATEGORIES 判斷**——判斷時
   優先參考該議題關鍵字命中的上下文（例如文章中提到「亞洲資產管理
   中心」的那段文字前後，比對是否同時出現政策與法規類或同業動態類
   的關鍵字），若無法區分上下文，則退回用全文（標題+摘要）跑一次
   通用判斷，每個議題各自套用同一個判斷結果
3. 子分類同樣採純字串比對，判斷依據優先順序：標題 > 前 200 字摘要。
   單一議題底下的子分類**強制單選**（同一議題不會同時屬於兩個子分類；
   若同時命中多類，取比對優先序較高者，優先序：政策與法規 > 商品與
   業務 > 同業動態；若皆未命中則標記為 `null`，交由人工審核判斷）

```python
SUBCATEGORIES = {
    "政策與法規": [
        "金管會", "法規", "鬆綁", "政策", "修法", "草案", "函釋",
        "核准", "試辦", "展延", "許可", "規範", "辦法",
    ],
    "商品與業務": [
        "商品", "業務", "推出", "上線", "開戶", "申購", "基金",
        "保單", "信託", "理財", "帳戶", "平台",
    ],
    "同業動態": [
        "金控", "銀行", "業者", "簽署", "合作", "MOU", "招商",
        "說明會", "布局", "進軍", "設立",
    ],
}
```

**注意**：以上子分類關鍵字為初版種子詞，準確率預期不會很高（純字串比對
在「政策與法規」vs「同業動態」這類語意相近的分類上容易誤判，且「依
議題上下文分別判斷子分類」這個邏輯比單純判斷一次更容易出錯），這正是
「全量人工審核期」存在的原因——請見「審核機制」章節。初期實作時，若
「依上下文分別判斷」的邏輯複雜度過高，**可以先簡化成每個議題都套用
同一個全文判斷結果（即先做成選項一：整篇文章一個子分類套用到所有
議題），等審核資料累積足夠、觀察到明顯需要逐議題區分的案例時，再升級
為逐議題各自判斷**——但無論哪種實作程度，資料庫的儲存結構都必須從
一開始就設計成「文章-議題-子分類」的組合關係（見下方 Schema），才不會
之後要花力氣遷移資料。

## 審核機制（全量人工審核期，過渡性設計）

### 核心邏輯
- 爬蟲與規則分類引擎**照常運作**：每篇新抓到的文章都會跑過 TOPICS
  規則判斷所屬議題（可多個），再對每個議題分別跑 SUBCATEGORIES 判斷
  子分類，產生「規則建議」（一組「議題＋子分類＋命中關鍵字依據」的
  配對清單，一篇文章可能有多組）
- 但規則建議**不直接寫入正式表**，而是連同原文連結一起寫入
  `pending_review` 表，等待人工審核
- 使用者在「待審核」畫面對每篇文章的操作分兩個層級：
  1. **逐項操作**（針對清單中每一組「議題＋子分類」建議）：
     - **核准**：單獨採用這一組建議
     - **移除**：判定規則誤判，這篇文章其實不屬於這個議題
     - **補充議題**：手動新增規則沒建議的議題（子分類預設空白，
       需人工指定後才能核准）
  2. **整篇文章層級操作**：
     - **全部採用**：一次核准清單中所有尚未處理的建議組合
     - **刪除**：判定整篇文章都不相關（例如關鍵字誤觸命中導致
       整篇文章都是雜訊），不寫入正式表，並記錄刪除原因供之後
       檢討規則用（需二次確認）
  審核完成的判定標準是：該篇文章清單中**每一組建議都已被核准或
  移除**（或整篇被刪除），此時 `pending_review.status` 才更新為
  `approved` 或 `deleted`，正式寫入 `articles` / `article_
  classifications` 表（或不寫入，若整篇刪除）
- 每一次「核准」或「移除／補充議題後核准」的操作都要記錄下來，用於
  計算「規則準確率」，**準確率的計算單位是「議題＋子分類」這個組合，
  不是整篇文章**——例如一篇文章橫跨三個議題，其中兩組被核准（採用
  原始建議）、一組被移除後由使用者補充了不同的議題，這篇文章記為
  2 次正確、1 次錯誤，而不是整篇文章記為一次「部分正確」：
  - 若某組建議被「核准」且子分類未經修改 → 該組合記為規則判斷正確
    （對應 `pending_review_final.was_suggested = 1`）
  - 若某組建議被「移除」，或使用者「補充議題」新增了規則沒建議的
    組合，或核准前先修改了子分類 → 該組合記為規則判斷錯誤（對應
    `was_suggested = 0`）
  - 「刪除」整篇文章不計入準確率計算（這不是分類錯誤，是根本不該
    收錄的雜訊，但應該累計筆數作為「規則誤觸率」的參考指標）

### 資料庫 Schema 新增
```sql
CREATE TABLE pending_review (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    url TEXT UNIQUE NOT NULL,
    title TEXT NOT NULL,
    source TEXT NOT NULL,
    published_date TEXT,
    summary TEXT,
    raw_keywords TEXT,
    origin TEXT NOT NULL,             -- 'crawler' 或 'manual'（見「手動新增」）
    status TEXT DEFAULT 'pending',    -- pending / approved / edited / deleted
    reviewed_at TEXT,
    created_at TEXT
);

-- 規則建議的「議題＋子分類」配對，一篇 pending_review 文章可以有多列
CREATE TABLE pending_review_suggestions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    pending_review_id INTEGER NOT NULL REFERENCES pending_review(id),
    suggested_topic TEXT NOT NULL,
    suggested_subcategory TEXT,       -- 可為 null（規則未命中子分類）
    suggested_reason TEXT             -- 命中的關鍵字依據，顯示給使用者參考
);

-- 使用者最終確認的「議題＋子分類」配對，審核完成後才寫入
-- （核准時直接複製 pending_review_suggestions 對應列的內容；
--   補充議題或修改子分類後核准時，寫入使用者調整後的結果）
CREATE TABLE pending_review_final (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    pending_review_id INTEGER NOT NULL REFERENCES pending_review(id),
    final_topic TEXT NOT NULL,
    final_subcategory TEXT,
    was_suggested INTEGER NOT NULL    -- 1=完全核准規則建議的這一組合,
                                       -- 0=人工新增或修改過，用於準確率統計
);
```

**「移除」的建議不會出現在 `pending_review_final`**：一組規則建議
若被使用者按下叉號移除，代表這篇文章根本不屬於這個議題，不會有任何
資料寫入 `pending_review_final`。因此「規則誤觸率」可以這樣推導：
該次審核 `pending_review_suggestions` 的總列數，減去最終真的被核准
（不論是否修改過）而出現在 `pending_review_final` 的列數，即為被
移除的建議數——不需要額外的欄位或表格記錄「移除」這個動作本身。

`articles` 正式表需要拆成主表＋分類關聯表，才能支援「一篇文章對應
多組議題＋子分類」：
```sql
CREATE TABLE articles (
    url TEXT PRIMARY KEY,           -- 原始新聞連結，前端必須可直接點擊開啟
    title TEXT NOT NULL,
    source TEXT NOT NULL,           -- '工商時報' or '經濟日報'
    published_date TEXT NOT NULL,   -- YYYY-MM-DD
    summary TEXT,                    -- 前 150 字，不存全文（省空間）
    raw_keywords TEXT,               -- 原始 meta-keywords，逗號分隔
    origin TEXT NOT NULL,            -- 'crawler' 或 'manual'
    crawled_at TEXT
);

-- 一篇文章可以對應多列，每列是一組「議題＋子分類」
CREATE TABLE article_classifications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    article_url TEXT NOT NULL REFERENCES articles(url),
    topic TEXT NOT NULL,             -- 六大議題之一
    subcategory TEXT,                -- 政策與法規／商品與業務／同業動態
    UNIQUE(article_url, topic)       -- 同一篇文章同一議題只能有一列
                                       -- （但可對應多個不同議題）
);
```
前端渲染依議題檢視時，用 `topic` 欄位篩選 `article_classifications`
表找出該議題所有文章，再 JOIN `articles` 表取得文章詳細內容；同一篇
文章若橫跨多個議題，會在不同議題的檢視畫面中各自出現，且各自顯示
該議題底下判定的子分類（可能不同）。

### 分類準確率統計（輔助判斷何時可切自動分類）
- 用途：讓使用者按**議題**分別檢視規則準確率，逐議題判斷是否已經穩定到
  可以切換為自動分類（不需人工逐篇確認），而不是六個議題綁在一起切換
- 計算方式：近 30 天內，`pending_review_final` 表中 `final_topic` 為
  該議題的所有列，`was_suggested = 1`（完全採用規則建議）的列數 ÷
  該議題的總列數。統計單位是「議題＋子分類」組合（一列），不是整篇
  文章——因為一篇橫跨多議題的文章，可能在某個議題判斷正確、在另一個
  議題判斷錯誤，這樣才能精準反映每個議題各自的規則準確度
- 呈現方式：待審核畫面下方一個可展開區塊，六大議題各一條進度條顯示
  準確率百分比，並附上建議門檻說明文字（例如「單一議題連續穩定達 90%
  以上時，可考慮切換該議題為自動分類」）
- 可直接從 `pending_review_final` JOIN `pending_review`（取得
  `reviewed_at` 時間）聚合計算，不需要額外建表

### 爬蟲涵蓋率統計（獨立於分類準確率之外的另一種指標）
- 用途：分類準確率量的是「規則判斷的議題對不對」，爬蟲涵蓋率量的是
  「爬蟲有沒有把新聞找到」——這是兩個性質不同的問題，**必須分開呈現**，
  不要合併成同一組數字，以免使用者誤判問題出在哪裡
- 統計單位是**文章**（不是議題＋子分類組合），因為涵蓋率關心的是
  「這篇文章爬蟲有沒有抓到」，跟文章橫跨幾個議題無關
- 計算方式：近 30 天內，`origin = 'crawler'` 的文章篇數 ÷
  （`origin = 'crawler'` 篇數 ＋ `origin = 'manual'` 篇數）。
  邏輯是：使用者手動新增的文章，代表「爬蟲當初沒抓到、是漏抓」，所以
  手動新增的數量越多，代表爬蟲涵蓋率越低，需要檢視是否要擴充關鍵字庫
  或修正該來源網站的抓取邏輯
- 呈現方式：待審核畫面下方另一個獨立區塊（與分類準確率區塊分開，但
  兩者都收在同一個可展開的統計區域內），顯示三個數字：近30天自動爬取
  則數、手動新增（爬蟲漏抓）則數、涵蓋率百分比，並附上說明文字

### 手動新增新聞（補齊爬蟲漏抓）
- 使用者自行在工商時報／經濟日報網站上看到爬蟲沒抓到的新聞時，可以在
  「待審核」畫面頂部的「手動新增新聞」區塊，貼上新聞連結＋選擇議題
  （六大議題下拉選單，**可複選**，因為手動新增的文章也可能橫跨多個
  議題）＋按下新增
- 系統流程：
  1. 使用者提交連結＋議題（一個或多個）後，後端立即嘗試抓取該 URL 的
     標題、來源（依網域判斷工商時報或經濟日報）、發布日期、150字摘要、
     meta-keywords（若有）——複用跟每日爬蟲相同的抓取邏輯函式，
     不要另外寫一套解析器
  2. 抓取成功後，寫入 `pending_review` 表（`origin = 'manual'`），並為
     使用者指定的**每一個議題**各寫入一列 `pending_review_suggestions`，
     `suggested_topic` 為該議題、`suggested_subcategory` 交給規則引擎
     針對該議題嘗試判斷（或留 null，交由使用者在審核卡片上手動指定）
  3. 若該 URL 抓取失敗（網站改版、非本站連結等），提示使用者錯誤訊息，
     不寫入資料庫，讓使用者確認連結是否正確
  4. 若該 URL 已存在於 `articles` 或 `pending_review` 表中（重複），
     提示「這則新聞已經收錄」，不重複寫入
- 手動新增的文章一樣要走「待審核」流程，不能繞過人工確認直接進正式表
  ——這是為了維持資料品質一致性，即使是使用者自己指定的議題，仍應該
  在審核卡片上確認一次（尤其是子分類可能需要人工指定）

### 刪除機制（含二次確認）
- 待審核卡片上的「刪除」是破壞性動作，UI 上點擊後**不直接刪除**，而是
  跳出行內的二次確認提示（原本三個動作按鈕暫時替換成一條確認提示：
  「確定要刪除這則新聞嗎？此動作無法復原。」+「取消」/「確定刪除」
  兩個按鈕），使用者按下「確定刪除」才真正執行刪除
- 刪除後該篇文章的 `status` 設為 `'deleted'`（軟刪除，保留紀錄供之後
  檢討規則庫用，不做實體刪除），不會出現在正式 `articles` 表中，也不
  再顯示於待審核清單

### 切換為自動分類的判斷方式（未來功能，本階段先確保資料結構支援）
- 本階段不需要做「自動切換」的程式邏輯，但資料庫設計需要支援未來實作
  ——即 `TOPICS` 規則字典需要能標記「此議題目前為自動模式或人工審核
  模式」的開關（可以用一個簡單的 config 檔案或資料表欄位，例如
  `topic_review_mode` 表：`topic TEXT PRIMARY KEY, mode TEXT DEFAULT
  'manual_review'`），当某議題手動切換為 `'auto'` 後，該議題的文章
  才會跳過待審核直接進入正式表
- 這個切換本階段設計為**使用者手動觸發**（不是系統自動判斷），即使
  準確率數字達標，也需要使用者自己按下切換，不會不告知就默默改變
  行為）

## 規則未命中文章的處理（併入「全量人工審核期」機制）

在「全量人工審核期」下，不論規則有沒有命中，所有文章都會進入
`pending_review` 表等待人工審核（見上方「審核機制」章節），因此不再
需要獨立的「未分類文章」流程或 `unclassified_review` 表——規則未命中
只是 `pending_review_suggestions` 裡某個議題的 `suggested_subcategory`
為 `null`（或該篇文章完全沒有任何 `pending_review_suggestions` 列，
代表連議題都判斷不出來）的特例，一樣顯示在待審核清單，只是審核卡片上
的「規則建議」文字會顯示「無命中，尚無對應關鍵字，需人工判斷分類」，
提示使用者這篇需要完全靠人工判斷（不是輔助對照規則建議）。

**未來若切換為「部分自動分類」模式**（見「切換為自動分類的判斷方式」）：
到時規則未命中的文章仍然需要人工審核（無法自動判斷代表規則庫還沒
涵蓋這種情況），只有規則命中且該議題已切換為自動模式的文章才會跳過
審核直接入庫；這也是為什麼即使切換自動模式，「待審核」畫面仍會保留
使用，用來處理規則庫的漏網之魚，並持續累積關鍵字擴充的依據。

## 每日排程（GitHub Actions）
- `.github/workflows/daily-crawl.yml`，cron 設定每天固定時間（例如台灣時間
  早上 7:00，換算 UTC 為前一天 23:00）觸發 `run_daily.py`
- 每次執行：對每個 SEED_TERM 呼叫搜尋頁，抓當天新增文章，去重（用 url
  UNIQUE 檢查是否已存在於 `articles` 或 `pending_review`），跑 TOPICS
  規則判斷該文章屬於哪些議題（可能多個），再對每個命中的議題分別跑
  SUBCATEGORIES 判斷子分類，寫入 `pending_review` 表（`origin='crawler'`,
  `status='pending'`）與對應的 `pending_review_suggestions`（一篇文章
  可能對應多列，每列一個議題＋子分類組合）——**不直接寫入 `articles`
  正式表**，等待人工審核（見「審核機制」章節）；最後產生執行報告，
  commit 回 repo
- log 記錄每次抓到幾篇、失敗幾篇，作為除錯依據

## 前端儀表板需求
- 純網頁（HTML/CSS/JS 即可），從 repo 內的 JSON 檔讀資料，用 GitHub Pages 部署
- **檢視切換放在底部導覽列**，不放在頁面上方——四個項目：日期／議題／趨勢／待審核
- 四種檢視：
  1. **依日期（日曆檢視）**：
     - 顯示月曆外觀，可以直接點選某一天
     - 有新聞的日期用小圓點標示在日期數字**正下方**（不是數字旁邊）
     - 點選某天後，下方列出當天所有文章（標題可點擊連到原文、來源、
       議題標籤）
     - **一篇文章橫跨多個議題時，該則新聞項目要並列顯示所有對應的
       議題標籤**（從 `article_classifications` 表依 `article_url`
       撈出該文章的所有議題列），不是只顯示一個；標籤數量多時該列
       需要能自然換行（`flex-wrap`），不能被擠出畫面或截斷
  2. **依議題（單一月份檢視＋當月重點報告）**：
     - 上方六個議題分頁籤（水平可捲動），選擇後只看該議題
     - 月份選擇用**輸入框**（格式 YYYYMM，例如 202609），不做左右箭頭
       翻月或無限捲動列表——使用者直接輸入年月跳轉到該月，搭配一個
       小箭頭圖示按鈕確認跳轉
     - 選定月份後顯示該月結構：
       - **當月重點報告**：一段摘要文字，說明該月此議題討論了什麼、
         方向有無轉變（見下方「月度/趨勢報告生成機制」），視覺上用
         比主背景稍深的沙色區塊呈現
       - 該月新聞篇數
       - 該月所有相關新聞清單（標題可點擊連到原文）
     - 若該月無相關新聞，顯示「本月無相關新聞」而非空白
     - **一篇文章若同時橫跨其他議題**：因為畫面本身已經是篩選過單一
       議題的檢視，清單裡不重複顯示「目前所在的這個議題」名稱（避免
       累贅），但要用一個淡色次要提示標示「同時歸類於＿＿」，列出
       該文章在其他議題下的歸類，讓使用者知道這篇文章不只出現在
       這個議題分頁——同一篇文章會分別出現在它所屬的每一個議題分頁下
  3. **趨勢洞察（新增，獨立於依議題之外）**：
     - 六個議題分頁籤（與依議題共用）
     - 呈現「跨月/跨季的敘事演變軌跡」，**由新到舊排序**（最新階段
       在最上面）：不是單月摘要的重複展示，而是進一步濃縮成幾個
       階段性描述，說明該議題的討論方向如何隨時間推進（例如：
       地方擴散檢視期 → 法規鬆綁量化期 → 政策倡議期，新到舊）
     - 每個階段用一個時間標籤（如「2026 Q3（最新）」）+ 一段描述文字，
       描述文字前方額外加一個子分類小標籤（政策與法規／商品與業務／
       同業動態其中之一），標示這個階段的變化主要落在哪個面向——
       **標籤只是標示，階段描述文字本身不拆成三段**
     - 這是「摘要之上的摘要」，需要先累積至少數個月的月度重點報告
       才能生成有意義的趨勢報告
     - 底部附一段「洞察說明」文字，用比主背景稍深的沙色區塊呈現，
       說明這份報告是彙整月度摘要生成、建議更新頻率等背景資訊
  4. **待審核（獨立成一個檢視，含以下四個子功能）**：
     - **手動新增新聞**：畫面頂部一個區塊，使用者可貼上新聞連結＋
       選擇議題（**可複選**，因為手動新增的文章也可能橫跨多個議題）
       ＋送出，系統自動抓取標題/來源/日期/摘要並存入待審核佇列
       （詳見「手動新增新聞」章節）
     - **待審核卡片清單**：每張卡片對應一篇文章，內含**「議題建議
       清單」**——一篇文章可能同時列出多組「議題＋子分類」建議
       （見下方詳細規格），並提供整篇文章層級的兩個操作：「全部
       採用」（一次核准清單裡所有議題組合）、「刪除」（整篇文章
       不收錄，需二次確認，見下方）
     - **議題建議清單的逐項操作**：清單中每一組「議題＋子分類」
       建議各自有兩個小圖示按鈕：
       - 勾選圖示：單獨核准這一組（若該組子分類為 null，需先由
         使用者在該列手動選擇子分類才能核准）
       - 叉號圖示：移除這一組建議（代表這篇文章其實不屬於這個
         議題，規則誤判了）
     - **補充議題**：清單下方有一個「補充議題」的下拉選單＋新增
       按鈕，讓使用者為這篇文章手動加上規則沒抓到的議題（新增後
       該組的子分類預設為空，需人工指定）
     - 若規則完全沒有命中任何議題，議題建議清單顯示提示文字「規則
       無命中任何議題，需人工判斷分類」，直接顯示「指定議題」的
       下拉選單讓使用者從零開始指定
     - **刪除的二次確認**：點擊刪除後，該卡片的操作按鈕原地替換成
       確認提示（「確定要刪除這則新聞嗎？此動作無法復原。」+ 取消/
       確定刪除兩個按鈕），避免手滑誤刪
     - **統計區塊**（放在畫面最下方，預設收合，點擊展開）：
       - 分類準確率：按六大議題分別統計規則建議被採用的比例（統計
         單位是「議題＋子分類」組合，不是整篇文章，見「分類準確率
         統計」章節）
       - 爬蟲涵蓋率：獨立於分類準確率的另一個指標，統計自動爬取與
         手動新增的比例
       兩者是不同性質的指標，UI 上分成兩個獨立卡片區塊呈現，不合併
- 每則新聞標題本身就是超連結，點擊直接開啟原始新聞頁面（新分頁開啟）
- 議題標籤要有顏色區分，方便一眼辨識
- 依議題畫面內，當月新聞清單上方要有子分類篩選 chip（全部／政策與
  法規／商品與業務／同業動態），點擊篩選只顯示該子分類的新聞；每則
  新聞項目上也要顯示一個小的子分類標籤（該子分類是「這篇文章在目前
  這個議題下」的子分類，不是文章的全域子分類——因為同一篇文章在
  不同議題下子分類可能不同）
- 需要簡單的關鍵字搜尋 filter（前端 JS 純文字比對即可，不需要後端）

## 月度/趨勢報告生成機制（使用 Claude API，非純規則）

考量到摘要品質，這兩層報告改用 Claude API 定期生成，而非純規則式自動產生。
由於執行頻率是月度/季度（而非每日），token 成本可控。

### 1. 月度重點報告（三段式，依子分類拆分）
- **觸發時機**：每月初（例如每月 1 號），對上個月「該議題底下所有文章」
  跑一次生成——從 `article_classifications` 表以 `topic` 篩選出該議題
  當月所有文章（JOIN `articles` 取得標題/摘要），**子分類依
  `article_classifications.subcategory` 分組**（因為同一篇文章在不同
  議題下的子分類可能不同，這裡只看該議題底下記錄的子分類，不是文章
  的全域子分類）
- **輸入給 Claude 的內容**：當月該議題所有文章的「標題 + 150字摘要」
  （不送全文），依上述子分類分成三組傳入，prompt 說明「請根據以下依
  三個類別分組的新聞標題與摘要，分別為政策與法規、商品與業務、同業
  動態三個類別各寫一段 60-80 字的重點摘要，說明該類別本月討論了什麼、
  有無明顯轉折；若某類別當月沒有新聞，該段落請填『本月無相關新聞』」
  ——**一次 API 呼叫產出三段文字**，不要拆三次呼叫（三段共用同一次
  上下文，且成本更低）
- **輸出**：三段摘要文字，存入新的資料表：
  ```sql
  CREATE TABLE monthly_reports (
      topic TEXT NOT NULL,
      year_month TEXT NOT NULL,        -- YYYY-MM
      policy_report TEXT,              -- 政策與法規段落
      product_report TEXT,             -- 商品與業務段落
      peer_report TEXT,                -- 同業動態段落
      article_count INTEGER,
      generated_at TEXT,
      PRIMARY KEY (topic, year_month)
  )
  ```
- 若某議題當月**完全沒有文章**（三個子分類都沒有），則不生成報告
  （前端該月份區塊顯示「本月無相關新聞」）；若只是某個子分類沒有
  文章，該子分類段落仍顯示「本月無相關新聞」，其他有資料的子分類
  正常生成

### 2. 趨勢報告（跨月，維持整合敘述，只在每階段加子分類標籤）
- 趨勢報告的階段性描述**不拆三段**，維持整合的敘事文字（跟月度報告
  不同），但每個階段除了時間標籤外，額外由 Claude 判斷這個階段的
  變化主要落在哪個子分類面向，產出一個子分類標籤一併附上
- **觸發時機**：每季一次（例如每年1、4、7、10月初），或月度報告累積
  達一定數量後手動觸發
- **輸入給 Claude 的內容**：該議題**過去所有月度重點報告的文字**
  （不是原始新聞全文，是已經濃縮過的月度摘要，進一步降低 token 用量，
  且輸入時附上各段對應的子分類標記，方便 Claude 判斷主要面向），
  加上 prompt 說明「請根據以下按時間排序的月度摘要，整理出幾個階段性
  的敘事演變描述，說明這個議題的討論方向如何隨時間推進，語氣中性、
  聚焦政策推進脈絡，非條列式新聞回顧；每個階段請額外標注一個主要
  子分類（政策與法規／商品與業務／同業動態三選一），代表這個階段的
  變化主要落在哪個面向」
- **輸出**：存入資料表（`sort_order` 數字越大代表越新，前端呈現時
  由新到舊排序，最新階段顯示在最上面）：
  ```sql
  CREATE TABLE trend_reports (
      topic TEXT NOT NULL,
      period_label TEXT NOT NULL,  -- 如「2025 Q1」「2025 Q4－2026 Q1」
      primary_subcategory TEXT,    -- 該階段主要落在哪個子分類（標籤用）
      stage_description TEXT NOT NULL,
      generated_at TEXT,
      sort_order INTEGER
  )
  ```
- 這一層是「摘要的摘要」，刻意不讓 Claude 重新讀取原始新聞全文，
  只讀月度摘要文字，維持低成本

### 成本控制原則
- 月度/趨勢報告的生成**都是批次排程觸發**（GitHub Actions 也可以設定
  月度/季度的 cron），不會在使用者瀏覽網頁時即時呼叫 API
- 每次生成呼叫的輸入都是「已濃縮過的文字」而非原始新聞全文，逐層降低
  token 用量：原始新聞全文 → （不使用）150字摘要 → 月度摘要 → 趨勢摘要
- 若某次生成結果不理想，允許人工在 admin 端手動觸發重新生成單一
  月份/議題的報告（不需要整批重跑）

## 視覺設計規範（高質感編輯風，已定案）

儀表板本質是「資料密集工具」（日曆、議題分頁、清單、待審核區塊），
不是沉浸式閱讀型 app，因此下列規範聚焦在配色、字體層級、留白與質感，
不套用「單篇文章沉浸閱讀」的部分（例如大圖 hero story、文章內頁排版）
——因為本儀表板的文章一律連結到外部原文，不在站內做全文閱讀頁。

以下規格已透過實際 HTML mockup 反覆確認定案，Claude Code 應直接照此
實作，不需要重新發想設計方向。若要參考完整視覺效果，可請使用者提供
先前確認過的 mockup HTML 檔案作為實作對照。

### 語言與在地化
- 介面文字一律使用繁體中文，議題標籤、日期、按鈕文案都用道地中文
  用語，不要英文直譯腔

### 色彩系統（採用低飽和、溫暖編輯感調性）
```css
--bg-page: #F3EEE4;       /* 米杏色主背景 */
--bg-panel: #EAE2D2;      /* 淺沙色次要區塊 */
--bg-panel-deep: #E3D9C4; /* 稍深沙色，用於待審核／重點報告區塊 */
--accent: #A9683F;        /* 赭褐強調色 */
--accent-deep: #7C4B2A;   /* 深赭褐，用於選中狀態文字 */
--text-main: #3A2E22;     /* espresso 炭褐，取代純黑 */
--text-sub: #7D7263;      /* 次要文字 */
--text-muted: #A69C8C;    /* 弱化文字／placeholder */
--divider: #D8CDB8;       /* 暖灰分隔線 */
```
- 六個議題標籤各用不同深淺的赭褐/暖色系做區分（例如 rgba 透明度或
  同色系不同色階），避免高飽和撞色，背景用淡色調（透明度 12-18%
  左右的赭褐色系），文字用對應的深色調
- 整體避免：霓虹色、強對比色塊、漸層、玻璃擬態（glassmorphism）、
  厚重陰影

### 字型
- 中文標題／品牌字：襯線字體（如 Noto Serif TC），營造編輯感
- 內文／中繼資訊：無襯線字體（如 Noto Sans TC）
- 每個畫面控制在 2–3 種字級，維持一致性：
  - 新聞標題：較大字級、襯線字體，是清單中最主要的視覺元素
  - 中繼資訊（來源、日期、議題標籤）：小字級，明顯區別於標題
  - 摘要／報告文字：中等字級，舒適行距（line-height 約 1.6-1.7）

### 站名標題設計（已定案樣式）
品牌標題採「直式英文小標 + 橫排中文大標」的雜誌式編排，四個畫面
（日期／議題／趨勢／待審核）的 header 統一使用此樣式：
- 左側：英文名稱以 `writing-mode: vertical-rl` 直式排列，內容為
  「BANK」「SINOPAC」兩段（用換行分成兩欄並排，不是硬擠一欄拉長），
  字級約 9px、字距約 1.5px、顏色用 `--text-sub`，右側加一條細直線
  （`border-right: 1px solid var(--divider)`）做分隔，高度不設死值，
  讓內容自然撐開並與右側中文標題垂直置中對齊
- 右側：中文站名「新聞觀測站」四字**同一排橫排**（不換行），襯線
  字體、字級約 21px、字距約 3px、顏色用 `--text-main`；下方副標
  「金融政策彙整」，無襯線字體、小字級（約 10.5px）、顏色用
  `--accent-deep`
- 此標題不含 emoji 或裝飾性圖片，純文字與線條構成

### 月份輸入框（依議題畫面使用，已定案樣式）
- 不做左右箭頭切換或下拉選單，改用一個**文字輸入框**：
  - placeholder 顯示淡灰色的 `YYYYMM` 格式提示（`opacity: 0.7` 左右，
    數值輸入後提示消失）
  - 輸入框本身無外框、無底色，只在下方有一條細的 `border-bottom`
    （用 `--divider` 顏色），底線寬度只等於輸入框本身寬度，**不要
    延伸到旁邊的確認按鈕下方**
  - 字體用襯線字體，字級適中（約 14px），置中對齊，寬度抓足夠餘裕
    避免 6 個字元被裁到邊緣
  - 旁邊搭配一個小巧的圓形確認按鈕：細框圓圈（`border: 1px solid
    var(--accent)`，直徑約 20px）內置一個向右箭頭線條圖示（不是
    文字「跳至」），點擊後才觸發月份跳轉

### 圖示規範
- 不使用 emoji（📅🔍✦📈 等）
- 統一使用細線條風格的 SVG 圖示（`stroke` 而非 `fill`，`stroke-width`
  約 1.6-1.8），包括：搜尋欄放大鏡圖示、底部導覽四個圖示（日曆、
  標籤、走勢折線、勾選框）、月份輸入框的確認箭頭
- 圖示顏色跟随所在情境的文字顏色（一般狀態用 `--text-muted`，選中
  狀態用 `--accent-deep`）

### 版面與互動細節
- **四種檢視的切換放在底部固定導覽列**（日期／議題／趨勢／待審核），
  不放在頁面頂部，頂部 header 只放品牌標題與搜尋欄（搜尋欄僅在
  依日期畫面顯示，其餘三個畫面不需要）
- 日曆檢視：月曆本身走簡約線條風，不用厚重格線；有新聞的日期用小
  圓點標示在**日期數字正下方**——注意 CSS 實作時務必讓日期格容器
  設為 `flex-direction: column`，讓數字與圓點兩個元素垂直堆疊，
  避免圓點被誤放在數字右側；點選某天後下方清單以溫暖的細分隔線
  區隔每則新聞，不用卡片外框；**單則新聞的 meta 資訊列（議題標籤＋
  來源）要設定 `flex-wrap: wrap`**，因為橫跨多議題的文章會並列
  顯示多個議題標籤，標籤數量多時需要能自然換到下一行，不能造成
  橫向溢出或被截斷
- 議題檢視：六個議題分頁籤走「水平可捲動 tab」的編輯感設計，選中
  狀態用底線＋字重變化表示，不要用強烈色塊；當月重點報告用比主
  背景稍深的沙色區塊（`--bg-panel-deep`）呈現，內部再依三個子分類
  分段落，每段前方用一個小圓點+子分類名稱做輕量標籤（不使用強烈
  色塊分隔三段，只靠留白與小標籤區分），與底下的原始新聞清單做出
  「摘要 vs 原文」的層次區隔；報告下方是子分類篩選 chip（全部／
  政策與法規／商品與業務／同業動態），選中狀態用實心赭褐底、白字，
  未選中用淺沙底、次要文字色；每則新聞項目的 meta 列上顯示該文章
  「在目前這個議題下」的子分類標籤（淡赭褐底色、深赭褐文字，圓角
  膠囊造型，比議題標籤更輕量，避免視覺上喧賓奪主）；**若該文章同時
  橫跨其他議題**，額外加一個更淡色（次要文字色、不用底色）的小提示
  文字「同時歸類於＿＿」，列出其他議題名稱，並附一個小圖示（例如
  連結／分支圖示）區別於子分類標籤，避免使用者誤以為是同一種標籤
- 趨勢洞察：階段性描述由新到舊排序，每個階段左側一個小時間標籤、
  緊接著一個子分類標籤（用跟議題畫面同樣的膠囊樣式），右側描述文字，
  避免過度圖像化的時間軸元件，保持文字為主、版面簡潔
- 待審核：
  - 頂部「手動新增新聞」區塊用淺沙背景卡片，內含連結輸入框（細
    底線風格，不用外框）、議題下拉選單（複選）、赭褐色送出按鈕
  - 待審核卡片清單：每張卡片用淺沙背景（`--bg-panel`），標題用襯線
    字體加粗、可點擊（旁附小外部連結圖示），下方一行來源與日期，
    再下方是**「議題建議清單」**——每一組「議題＋子分類」建議各佔
    一列，列與列之間用極細的分隔線區隔；每列內容為：議題名稱（較大
    字級、加粗）、子分類名稱（赭褐色文字；若無命中則用弱化文字色
    顯示「子分類無命中，需人工指定」）、命中關鍵字依據（更小字級、
    弱化文字色）；每列右側有兩個小圖示按鈕：勾選圖示（單獨核准這
    一組，用赭褐色）、叉號圖示（移除這一組建議，用次要文字色，
    hover 時可變深色提示可操作）
  - 議題建議清單下方是「補充議題」的下拉選單＋文字連結風格的「＋
    新增」按鈕（赭褐色文字，無底色無外框），讓使用者手動加上規則
    沒抓到的議題；若規則完全無命中任何議題，整個清單只顯示一行
    弱化文字提示「規則無命中任何議題，需人工判斷分類」，直接接上
    「指定議題」的下拉選單
  - 卡片底部是整篇文章層級的操作列：「全部採用」（實心赭褐底，一次
    核准清單中所有建議組合）、「個別修改」（細框、次要文字色，進入
    逐項編輯模式）、「刪除」（細框、更淡的文字色、只放垃圾桶圖示不
    放文字，寬度固定較窄，視覺份量最輕，避免手滑誤觸的同時也不會
    搶走版面注意力）
  - 刪除二次確認：點擊刪除後，整篇文章層級的操作列原地替換成一條
    淡赭褐底色（比卡片背景再深一點、帶邊框）的確認提示列，文字＋
    取消／確定刪除兩個按鈕，不使用跳出式 modal 或遮罩，維持畫面輕量
  - 統計區塊（分類準確率、爬蟲涵蓋率）放在畫面最下方，各自獨立成
    一個卡片，與待審核卡片清單之間有明顯留白區隔，暗示這是「參考
    資訊」而非「待辦事項」；分類準確率用細長條圖呈現六個議題的
    百分比，爬蟲涵蓋率用簡單的數字＋說明文字呈現，不需要圖表
- 保持大量留白，避免資訊過度擁擠；即使是資料密集的清單，也要靠
  留白和分隔線維持「呼吸感」，而不是塞滿卡片
- 一次畫面只有一個主要視覺焦點（例如日曆檢視時月曆是焦點，清單是
  次要；議題檢視時當前月份的重點報告是焦點，新聞清單是支撐細節；
  待審核畫面時待處理的審核卡片是焦點，統計數據是畫面最下方的參考
  資訊，不搶注意力）

### 明確排除項目
- 不需要 hero story／編輯精選大圖區塊（不適用於資料儀表板）
- 不需要站內文章閱讀頁（含字級調整、書籤等閱讀控制項）——文章一律
  連結到外部原文開啟
- 不需要底部固定導覽列的「收藏」「我的」等個人化功能（除非之後有
  需求再加）
- 不使用左右箭頭式的月份翻頁或下拉選單——月份切換一律用 YYYYMM
  文字輸入框

## Token/成本優化原則（務必遵守）
1. 分類邏輯（議題與子分類）全部用 Python 字串比對完成，不呼叫任何
   LLM API——這在「全量人工審核期」下仍然成立，規則引擎只是產生
   「建議」，供人工審核參考，不代表放棄規則式分類這個省成本的核心
   設計
2. 資料庫只存摘要（150字內），不存全文
3. 手動新增新聞時的內容抓取（標題/來源/日期/摘要）複用爬蟲的抓取
   函式，不呼叫 LLM 做內容解析
4. 月度重點報告一次 API 呼叫產出三段（政策法規/商品業務/同業動態），
   不拆三次呼叫；趨勢報告只讀月度摘要文字，不重新讀取原始新聞全文
5. 前端純靜態 JSON + JS，不需要即時後端運算；分類準確率與爬蟲涵蓋率
   統計皆為資料庫聚合查詢，不需要額外的運算服務

## 待確認事項（執行時可能需要人工調整，遇到請記錄進執行報告一起討論）
- udn.com 的反爬蟲策略可能隨時變動，需要有 fallback（例如改用 RSS 或
  Google News 搜尋間接取得連結）
- 三軌金融、金融大回饋這兩個詞彙較新，關鍵字庫需要在專案跑起來後根據
  實際抓到的文章持續補充相關詞彙，透過每天的「待審核」流程逐步擴充
- GitHub Actions 的 cron 有時會有延遲（非即時觸發），這是 GitHub 平台本身
  的已知限制，執行時間可能與設定時間有數分鐘到數十分鐘落差，屬正常現象
- **子分類規則（SUBCATEGORIES）初版準確率預期偏低**：「政策與法規」
  與「同業動態」在語意上容易重疊（例如「金管會核准某銀行試辦」同時
  命中兩類的關鍵字），這是「全量人工審核期」設計的主要原因之一，
  預期需要多輪根據實際審核結果調整關鍵字與優先序判斷邏輯
- 「切換為自動分類」的實際觸發介面（例如儀表板上的開關按鈕）本階段
  未設計，只確保資料庫欄位（`topic_review_mode` 表）支援未來擴充，
  待累積一段時間審核資料、準確率穩定後再回來討論這個功能怎麼做
- **「逐議題分別判斷子分類」的實作複雜度需要在開發時評估**：若初期
  發現「依關鍵字命中上下文分別判斷」邏輯太複雜或不穩定，可以先簡化
  為「整篇文章跑一次子分類判斷、套用到所有命中的議題」（見「子分類
  規則」章節說明），但資料庫從一開始就用 `article_classifications`
  這種「文章-議題-子分類」關聯表結構，不要因為先簡化實作就跟著簡化
  資料庫設計，否則之後要精進判斷邏輯時會需要額外的資料遷移工作
